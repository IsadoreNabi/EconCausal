% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecm_mars.R
\name{ecm_mars}
\alias{ecm_mars}
\title{Error-correction screening with MARS forecasts under rolling-origin validation}
\usage{
ecm_mars(
  data_path,
  circ_vars,
  prod_vars,
  cointeg_rule = "either",
  eg_p_cutoff = 0.05,
  ecm_p_cutoff = 0.05,
  lag_max_ecm = 4,
  min_tr = 20,
  min_te = 8,
  rolling_cv_enable = TRUE,
  rolling_cv_window = "sliding",
  rolling_cv_initial_frac = 0.8,
  rolling_cv_initial_min = 40,
  rolling_cv_test = 12,
  rolling_cv_step = 12,
  nested_tune = TRUE,
  nested_initial_f = 0.6,
  nested_test = 6,
  nested_step = 3,
  mars_grid = expand.grid(degree = c(1, 2), nk = c(15, 25, 35, 50, 65)),
  support_min = 0.75,
  folds_min_abs = 5,
  parallel_enable = TRUE,
  parallel_workers = max(1, parallel::detectCores() - 1)
)
}
\arguments{
\item{data_path}{Character scalar. Path to an Excel file with exactly 14 columns in this
order: a date column followed by the 13 series \code{ER.SPOT.CAN.US}, \code{ER.SPOT.US.CAN},
\code{ER.SPOT.US.REMB}, \code{CPI}, \code{TreasuryBonds10y}, \code{FedDiscountRate}, \code{Exports},
\code{RealNetProfit}, \code{RealSocialConsumptionPerWorker2017}, \code{RealWagePPP2017},
\code{CapitalStockPPP2017}, \code{LaborProductivityPPP2017}, \code{InvestmentPerWorkerPPP2017}. The
columns are renamed to these names by position, whatever their names in the file.}

\item{circ_vars}{Character vector of circulation variable names, a subset of the 13 names
above. Names not present are dropped.}

\item{prod_vars}{Character vector of production variable names, a subset of the 13 names
above. Names not present are dropped.}

\item{cointeg_rule}{Character scalar, \code{"either"} (default) or \code{"both"}: whether
cointegration requires the Engle-Granger/Phillips-Ouliaris screen or the Johansen screen,
or both of them.}

\item{eg_p_cutoff}{Numeric scalar in (0, 1), default \code{0.05}. Significance level of the
residual unit-root test and of the Phillips-Ouliaris test.}

\item{ecm_p_cutoff}{Numeric scalar in (0, 1), default \code{0.05}. One-sided significance level
for the error-correction coefficient being negative.}

\item{lag_max_ecm}{Integer scalar >= 1, default \code{4}. Largest lag order of the differenced
series considered in the linear error-correction model.}

\item{min_tr}{Integer scalar >= 1, default \code{20}. Minimum number of complete training rows
required to fit MARS in a window.}

\item{min_te}{Integer scalar >= 1, default \code{8}. Minimum number of complete test rows required
to evaluate a window.}

\item{rolling_cv_enable}{Logical scalar, default \code{TRUE}. If \code{FALSE}, a single split with the
first 75 percent of observations for training is used instead of rolling windows.}

\item{rolling_cv_window}{Character scalar, \code{"sliding"} (default) or \code{"expanding"}. Training
window type of the outer validation.}

\item{rolling_cv_initial_frac}{Numeric scalar in (0, 1), default \code{0.8}. Fraction of the
sample in the first training window.}

\item{rolling_cv_initial_min}{Integer scalar >= 1, default \code{40}. Minimum size of the first
training window.}

\item{rolling_cv_test}{Integer scalar >= 1, default \code{12}. Test horizon of each outer window.}

\item{rolling_cv_step}{Integer scalar >= 1, default \code{12}. Advance between outer windows.}

\item{nested_tune}{Logical scalar, default \code{TRUE}. If \code{TRUE}, MARS degree and number of terms
are chosen in each outer training window by an inner validation; if \code{FALSE}, degree 2 and
25 terms are used.}

\item{nested_initial_f}{Numeric scalar in (0, 1), default \code{0.6}. Fraction of the outer
training window in the first inner training window (at least 30 rows).}

\item{nested_test}{Integer scalar >= 1, default \code{6}. Test horizon of each inner window.}

\item{nested_step}{Integer scalar >= 1, default \code{3}. Advance between inner windows.}

\item{mars_grid}{Data frame with numeric columns \code{degree} and \code{nk}, default
\code{expand.grid(degree = c(1, 2), nk = c(15, 25, 35, 50, 65))}. Candidate MARS settings.}

\item{support_min}{Numeric scalar in (0, 1], default \code{0.75}. Minimum proportion of outer
windows that must pass the screen for \code{pass_support}.}

\item{folds_min_abs}{Integer scalar >= 0, default \code{5}. Minimum absolute number of outer
windows that must pass the screen for \code{pass_support}.}

\item{parallel_enable}{Logical scalar, default \code{TRUE}. Evaluate directions in parallel R
sessions.}

\item{parallel_workers}{Integer scalar >= 1, default \code{max(1, parallel::detectCores() - 1)}.
Number of parallel sessions when \code{parallel_enable = TRUE}.}
}
\value{
A \code{data.frame} with one row per direction and columns \code{pair} (\code{"X -> Y"}), \code{Y}, \code{X},
\code{folds} (outer windows), \code{folds_proceed} (windows that passed the screen), \code{RMSE}, \code{MAE},
\code{MAPE}, \code{sMAPE}, \code{R2}, \code{TheilU}, \code{bias_prop}, \code{var_prop}, \code{cov_prop} (forecast metrics
averaged over the windows that passed), \code{support} (\code{folds_proceed / folds}),
\code{pass_support} (logical), \code{R2_stab} (\code{R2 * support}) and \code{U_stab} (\code{TheilU / support}),
sorted by decreasing \code{R2} and then by \code{pair}.
}
\description{
Evaluates every directed pair between a set of circulation variables and a set of production
variables, in both directions, and measures how often a non-linear error-correction forecast
is both statistically admissible and predictive across successive temporal windows. Each
window first screens the pair for integration, cointegration and a significant
error-correction term; only windows that pass that screen fit a Multivariate Adaptive
Regression Splines (MARS) forecaster. Use it to rank directed relationships by the temporal
stability of their error-correction structure rather than by a single full-sample fit.
}
\details{
In every training window the response \code{Y} and the predictor \code{X} must both be integrated of
order one: augmented Dickey-Fuller tests with lag order chosen by AIC fail to reject a unit
root in levels (with drift or with trend, 10 percent) and reject it in first differences.
The VAR lag order is chosen by the Schwarz criterion. Cointegration is assessed with the
Johansen trace test (constant, then trend, 5 percent) and with the Engle-Granger residual
test or the Phillips-Ouliaris test at \code{eg_p_cutoff}, combined by \code{cointeg_rule}. A linear
error-correction model is then fitted with the lag order that minimises BIC, preferring the
first order whose residuals pass a Ljung-Box test at lag 12, and the error-correction
coefficient must be negative with a one-sided p-value below \code{ecm_p_cutoff} using Newey-West
standard errors. The screen is deliberately strict because MARS is flexible enough to fit
spurious non-linear structure between non-cointegrated series.

Windows that pass fit MARS to the first difference of \code{Y} on the lagged error-correction
term, the first difference of \code{X}, the first lags of both differences and the second lag of
the difference of \code{Y}, and forecast the level as the
previous level plus the forecast difference. With \code{nested_tune = TRUE}, the MARS setting with
the lowest mean RMSE across inner expanding windows is used; the inner validation never sees
the outer test window.

\code{pass_support} requires \code{folds_proceed >= max(folds_min_abs, ceiling(support_min * folds))},
so both a relative and an absolute number of admissible windows are needed.
}
\section{Methodological notes}{

\code{support} measures temporal stability of the screen, not forecast accuracy: a direction can
forecast well in the few windows where it passes and still have low support. \code{R2} and the
other metrics are averaged only over windows that passed, so they must be read together with
\code{folds_proceed}. The error-correction term uses the Engle-Granger long-run coefficients
estimated in the training window, so no test-window information enters the forecaster.
Parallel evaluation uses reproducible random streams (\code{future.seed = TRUE}). The function
restores the caller's \code{future} plan and BLAS/OpenMP thread settings on exit.
}

\section{Dependencies}{

\code{readxl} reads the data; \code{dplyr} orders observations and builds lags; \code{urca} provides the
augmented Dickey-Fuller and Johansen tests; \code{vars} selects the VAR lag order; \code{tseries}
provides the Phillips-Ouliaris test; \code{lmtest} and \code{sandwich} give the Newey-West test of the
error-correction coefficient; \code{earth} fits MARS; \code{future} and \code{future.apply} evaluate
directions in parallel; \code{progressr} reports progress; \code{RhpcBLASctl}, if installed, keeps
BLAS and OpenMP single-threaded during the evaluation.
}

\examples{
\dontrun{
result <- ecm_mars(
  data_path = file.path(tempdir(), "data.xlsx"),
  circ_vars = c("ER.SPOT.CAN.US", "ER.SPOT.US.CAN", "ER.SPOT.US.REMB",
                "CPI", "TreasuryBonds10y", "FedDiscountRate"),
  prod_vars = c("Exports", "RealNetProfit", "RealSocialConsumptionPerWorker2017",
                "RealWagePPP2017", "CapitalStockPPP2017",
                "LaborProductivityPPP2017", "InvestmentPerWorkerPPP2017")
)
}

}
\references{
Engle, R. F., & Granger, C. W. J. (1987). Co-integration and error correction:
Representation, estimation, and testing. \emph{Econometrica, 55}(2), 251\enc{–}{-}276.
\doi{10.2307/1913236}

Friedman, J. H. (1991). Multivariate adaptive regression splines. \emph{The Annals of
Statistics, 19}(1), 1\enc{–}{-}67. \doi{10.1214/aos/1176347963}

Johansen, S. (1988). Statistical analysis of cointegration vectors. \emph{Journal of Economic
Dynamics and Control, 12}(2\enc{–}{-}3), 231\enc{–}{-}254. \doi{10.1016/0165-1889(88)90041-3}

Newey, W. K., & West, K. D. (1987). A simple, positive semi-definite, heteroskedasticity and
autocorrelation consistent covariance matrix. \emph{Econometrica, 55}(3), 703\enc{–}{-}708.
\doi{10.2307/1913610}

Phillips, P. C. B., & Ouliaris, S. (1990). Asymptotic properties of residual based tests for
cointegration. \emph{Econometrica, 58}(1), 165\enc{–}{-}193. \doi{10.2307/2938339}
}
\seealso{
\code{\link[=bsts_model]{bsts_model()}}, \code{\link[=bglmar1]{bglmar1()}}; the vignettes \code{ecm-mars-eng} and \code{ecm-mars-esp}.
}

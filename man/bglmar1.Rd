% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bglmar1.R
\name{bglmar1}
\alias{bglmar1}
\title{Bayesian generalized linear model with AR(1) errors and leave-future-out validation}
\usage{
bglmar1(
  data_path,
  circ_vars,
  prod_vars,
  max_lag = 3,
  initial_frac = 0.7,
  initial_min = 90,
  test_h = 12,
  step_h = 12,
  lfo_window = "sliding",
  chains = 4,
  parallel_chains = 4,
  iter = 1500,
  warmup = 750,
  adapt_delta = 0.95,
  trees = 12,
  seed = 2025,
  support_min = 0.6,
  folds_min = 5,
  sup_hi = 0.7,
  sup_lo = 0.6,
  backend = c("auto", "rstan", "cmdstanr")
)
}
\arguments{
\item{data_path}{Character scalar. Path to an Excel file with a date column and the series
named in \code{circ_vars} and \code{prod_vars}. Column names are cleaned before matching: a leading
\code{as.numeric.}, trailing \code{.NEW.} or \verb{.1.588.} and trailing dots are removed, and remaining
dots become underscores. A date column named \code{Month} is used, or the first date-time
column is renamed to \code{Month}.}

\item{circ_vars}{Character vector of exactly six circulation variable names, after cleaning.}

\item{prod_vars}{Character vector of exactly seven production variable names, after cleaning.}

\item{max_lag}{Integer scalar >= 1, default \code{3}. Number of lags of the predictor used as
regressors.}

\item{initial_frac}{Numeric scalar in (0, 1), default \code{0.7}. Fraction of the complete
observations that sizes the first training window; the size actually used is the larger of
that fraction and \code{initial_min}.}

\item{initial_min}{Integer scalar >= 1, default \code{90}. Lower bound of the first training
window.}

\item{test_h}{Integer scalar >= 1, default \code{12}. Number of observations in the test block of
each validation window; one year with monthly data.}

\item{step_h}{Integer scalar >= 1, default \code{12}. Advance of the end of the training sample
between windows.}

\item{lfo_window}{\code{"sliding"} (default) or \code{"expanding"}. With \code{"sliding"} the training
sample keeps at most the initial number of rows and moves forward; with \code{"expanding"} it
grows from the first observation.}

\item{chains}{Integer scalar >= 1, default \code{4}. Number of MCMC chains per model.}

\item{parallel_chains}{Integer scalar >= 1, default \code{4}. Number of chains run in parallel,
passed as the number of cores of the sampler.}

\item{iter}{Integer scalar > \code{warmup}, default \code{1500}. Total iterations per chain, warmup
included.}

\item{warmup}{Integer scalar >= 0 and < \code{iter}, default \code{750}. Warmup iterations per chain.}

\item{adapt_delta}{Numeric scalar in (0, 1), default \code{0.95}. Target acceptance rate of the
sampler.}

\item{trees}{Integer scalar >= 1, default \code{12}. Maximum tree depth of the sampler.}

\item{seed}{Integer scalar, default \code{2025}. Base seed; the model without lags is fitted with
\code{seed + 101} and the model with lags with \code{seed + 202}, in every window and for every pair.}

\item{support_min}{Numeric scalar, default \code{0.6}. Not used by the function; the support
thresholds applied to the returned rankings are \code{sup_hi} and \code{sup_lo}.}

\item{folds_min}{Integer scalar >= 0, default \code{5}. Minimum number of evaluated windows
required by \code{winners_070} and \code{winners_060}.}

\item{sup_hi}{Numeric scalar in [0, 1], default \code{0.7}. Support threshold of \code{winners_070}.}

\item{sup_lo}{Numeric scalar in [0, 1], default \code{0.6}. Support threshold of \code{winners_060}.}

\item{backend}{Character scalar, one of \code{"auto"} (default), \code{"rstan"} or \code{"cmdstanr"}. Engine
used to fit the models. With \code{"auto"} the function uses \code{rstan} when it is installed and
otherwise a working \code{cmdstanr}. The option \code{EconCausal.backend}, when set, overrides this
argument, and the function stops when neither engine is available.}
}
\value{
A list with four elements. \code{bench_bayes} is a tibble with one row per direction and
the columns \code{pair} (written as \code{"X -> Y"}), \code{folds}, \code{folds_pass}, \code{support}
(\code{folds_pass / folds}), \code{ELPD_diff_mean}, \code{RMSE_diff_mean}, \code{RMSE_full_mean},
\code{RMSE_base_mean}, \code{MAE_full_mean}, \code{MAE_base_mean}, \code{sMAPE_full_mean}, \code{sMAPE_base_mean},
\code{R2_full_mean} and \code{R2_base_mean}, sorted by decreasing \code{support}, decreasing
\code{ELPD_diff_mean} and increasing \code{RMSE_diff_mean}. \code{winners_070} and \code{winners_060} are the
rows of that tibble with at least \code{folds_min} evaluated windows, finite support of at least
\code{sup_hi} or \code{sup_lo}, positive \code{ELPD_diff_mean} and negative \code{RMSE_diff_mean}; their names
do not change with \code{sup_hi} and \code{sup_lo}. \code{rank_out} is a list with \code{all}, the sorted
benchmark, \code{winners_hi} and \code{winners_lo}, the two tibbles just described, and \code{ratios},
with \code{pair}, \code{support}, \code{ELPD_diff_mean}, \code{RMSE_diff_mean}, \code{RMSE_ratio} and \code{MAE_ratio},
sorted by increasing \code{RMSE_ratio}. A direction with too few observations, without any
validation window or without any evaluated window returns a row with \code{folds} equal to zero
and missing metrics.
}
\description{
Evaluates every directed pair between six circulation variables and seven production
variables, in both directions, by comparing a Bayesian regression of the response on a linear
time trend with and without lagged values of the predictor, both with first-order
autoregressive errors. Each comparison is repeated over successive forecast windows, and a
direction counts as supported in a window only when adding the lags improves both the
predictive density and the point forecast. Use it to rank directed relationships by how
consistently the predictor improves out-of-sample forecasts.
}
\details{
For each direction the predictor is lagged \code{max_lag} times and incomplete rows are dropped. A
direction is skipped when the remaining rows are fewer than \code{initial_min + test_h + max_lag + 5}. The first training sample holds the larger of \code{initial_min} rows and \code{initial_frac} of the
remaining rows, each test block holds the next \code{test_h} observations, and the end of the
training sample advances by \code{step_h} until the last observation is reached. In each window the
response, the time index and the lags are standardised with the training mean and standard
deviation, the response is returned to its original scale before the metrics are computed, and
a lag whose training standard deviation is zero is dropped from that window.

Two models are fitted to the standardised response with Gaussian errors, a normal prior on the
regression coefficients, a Student t prior on the intercept and an exponential prior on the
residual scale: a model on the standardised time index alone and a model that adds the
standardised lags of the predictor. Both carry a first-order autoregressive term on a single
series indexed by time, so the comparison isolates the contribution of the lags. For the test
block the window records the expected log predictive density of each observation, obtained as
the log of the mean of the posterior likelihood draws and summed over the block, together with
RMSE, MAE, symmetric MAPE and the coefficient of determination of the posterior mean forecast.
A window is a win when the model with the lags has both a higher predictive density and a
lower RMSE, and \code{support} is the share of wins among the evaluated windows.
}
\section{Methodological notes}{

\code{support} requires improvement in probabilistic fit and in point error at the same time; a
predictor that improves only one of them in a window does not count. The predictive density is
computed from the posterior likelihood draws of the test observations, with group-level terms
excluded, and not from a Gaussian approximation to the forecast. Standardising the response,
the time index and the lags with training statistics only keeps test-window information out of
the fit. The linear time trend enters both models, so a direction cannot be supported by the
trend itself. Windows in which the response does not vary, or in which a fit or a predictive
evaluation fails, are dropped and do not count in \code{folds}, so \code{support} is a proportion over
evaluated windows and not over possible ones. Both seeds are derived from \code{seed} and not from
the window or the pair, which makes a run reproducible for a given \code{seed}. The 84 directions
are ranked under a common criterion and without any correction for multiplicity: \code{support}
describes how consistent a single direction is across windows, and is not a joint test.
}

\section{Dependencies}{

\code{brms} builds the formulas and the priors, fits the models, and produces the pointwise
likelihood and the posterior expectation of the test block; the fitting engine is \code{rstan} or
\code{cmdstanr}, which are suggested packages chosen at run time and not imported. \code{readxl} reads
the data; \code{dplyr}, \code{tidyr} and \code{tibble} build lags, windows and summaries; \code{magrittr} supplies
the pipe and \code{rlang} the data pronoun; \code{stats} provides the standard deviation, the Gaussian
family and the formula built for each window; \code{parallel} counts the available cores for the
duration of the call; and \code{utils} writes the ranking tables when the internal ranking helper
is given output paths, which the defaults do not do.
}

\examples{
\dontrun{
result <- bglmar1(
  data_path = file.path(tempdir(), "data.xlsx"),
  circ_vars = c("TC_SPOT_CAN_US", "TC_SPOT_US_CAN", "TC_SPOT_US_REMB",
                "IPC", "TdI_LdelT", "TasaDescuento"),
  prod_vars = c("ValorExportaciones", "Real_Net_Profit",
                "RealSocialConsumptionPerWorker2017", "RealWage_PPP2017",
                "CapitalStock_PPP2017", "LaborProductivity_PPP2017",
                "InvestmentPerWorker_PPP2017"),
  backend = "auto"
)
}

}
\references{
\enc{Bürkner}{Burkner}, P.-C. (2017). brms: An R package for Bayesian multilevel models using
Stan. \emph{Journal of Statistical Software, 80}(1), 1\enc{–}{-}28.
\doi{10.18637/jss.v080.i01}

\enc{Bürkner}{Burkner}, P.-C., Gabry, J., & Vehtari, A. (2020). Approximate leave-future-out
cross-validation for Bayesian time series models. \emph{Journal of Statistical Computation and
Simulation, 90}(14), 2499\enc{–}{-}2523. \doi{10.1080/00949655.2020.1783262}
}
\seealso{
\code{\link[=bsts_model]{bsts_model()}}, \code{\link[=ecm_mars]{ecm_mars()}}; the vignettes \code{bglmar1-eng} and \code{bglmar-esp}.
}
